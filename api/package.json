{
  "name": "aws-s3",
  "version": "1.0.0",
  "description": "Backend",
  "main": "dist/infrastructure/express/server.js",
  "scripts": {
    "build": "rm -rf ./dist & tsc",
    "prestart": "yarn build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file=.env ."
  },
  "author": "Esteban Mariano Piga Alessi",
  "license": "UNLICENSED",
  "dependencies": {
    "aws-sdk": "^2.1670.0",
    "express": "^4.19.2",
    "typescript": "^5.5.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.2"
  }
}
